pipeline {
   agent { dockerfile true}
   stages {
     stage ('test')
       steps {
         sh '''
           git --version
           node --version
           curl --version
         '''
         
       }  
     }  
   }
 }  
